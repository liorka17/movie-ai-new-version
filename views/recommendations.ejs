<!DOCTYPE html> <!-- הגדרת המסמך כ־HTML5 -->
<html lang="he"> <!-- שפת הדף: עברית -->
<%- include('partials/head') %> <!-- כולל את קובץ ה-head (meta, title, css וכו') -->

<body>
  <%- include('partials/navbar') %> <!-- כולל את סרגל הניווט העליון -->

  <main class="gallery-container"> <!-- אותה קלאסה כמו הגלריה הרגילה כדי לשמור על אחידות עיצוב -->
    <h1>🎬 המלצות מותאמות עבורך</h1> <!-- כותרת עמוד -->

    <% if (message) { %> <!-- אם קיימת הודעה להצגה (למשל: "לא נמצאו המלצות") -->
      <p class="info-message"><%= message %></p>
    <% } %>

    <div class="movies-grid"> <!-- גריד להצגת כרטיסי הסרטים, בדיוק כמו בגלריה -->
      <% recommendations.forEach(movie => { %> <!-- לולאה על מערך הסרטים המומלצים -->
        <div class="movie-card"> <!-- כרטיס סרט -->
          <div class="movie-image"> <!-- אזור תמונה -->
            <img src="<%= movie.poster %>" alt="<%= movie.title %>"> <!-- פוסטר הסרט -->
          </div>
          <div class="movie-info"> <!-- פרטי הסרט -->
            <h3><%= movie.title %></h3> <!-- שם הסרט -->
            <p><%= movie.overview || "אין תקציר זמין" %></p> <!-- תקציר או ברירת מחדל -->
            <a href="/video/movie/<%= movie.id %>" class="btn-details">📽️ פרטים נוספים</a> <!-- קישור לעמוד הסרט -->
          </div>
        </div>
      <% }) %>
    </div>
  </main>

  <%- include('partials/footer') %> <!-- כולל את הפוטר -->
</body>
</html>
