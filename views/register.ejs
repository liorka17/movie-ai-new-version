<!DOCTYPE html>
<html lang="he">
<%- include('partials/head') %>

<body>
  <%- include('partials/navbar') %>

  <main class="register-container">
    <div class="register-box">
      <h2>🎟️ הרשמה למועדון הקולנוע</h2>
      <p class="register-subtitle">קבל גישה להמלצות סרטים מותאמות אישית 🍿</p>

      <form action="/user/register" method="POST" enctype="multipart/form-data" class="register-grid">
        <div class="input-group">
          <label for="username">שם משתמש 👤</label>
          <input type="text" id="username" name="username" required />
        </div>

        <div class="input-group">
          <label for="email">כתובת אימייל 📧</label>
          <input type="email" id="email" name="email" required pattern="[^@\s]+@[^@\s]+\.[^@\s]+" />
        </div>

        <div class="input-group">
          <label for="password">סיסמה 🔒</label>
          <input type="password" id="password" name="password" required minlength="6" />
        </div>

        <div class="input-group">
          <label for="fullName">שם מלא 🧑‍💼</label>
          <input type="text" id="fullName" name="fullName" />
        </div>

        <div class="input-group">
          <label for="birthday">תאריך לידה 🎂</label>
          <input type="date" id="birthday" name="birthday" />
        </div>

        <div class="input-group">
          <label for="favoriteGenre">ז'אנר מועדף 🎬</label>
          <input type="text" id="favoriteGenre" name="favoriteGenre" />
        </div>

        <div class="input-group">
          <label for="phone">מספר טלפון 📱</label>
          <input type="tel" id="phone" name="phone" pattern="^0[5-9]\d{7,8}$" placeholder="05X-XXXXXXX" />
        </div>

        <div class="input-group full-width">
          <label for="profileImage">תמונת פרופיל 📷</label>
          <input type="file" id="profileImage" name="profileImage" accept="image/*" />
        </div>

        <div class="full-width">
          <button type="submit" class="btn-register">✨ הצטרף עכשיו</button>
        </div>
      </form>

      <p class="login-link">כבר רשום? <a href="/login">🎬 התחבר עכשיו</a></p>
    </div>
  </main>

  <%- include('partials/footer') %>
</body>
</html>
